{{ define "response" }}
[
    {{ range $index, $e := .Data.Pages }}
    {{ if $index }}, {{ end }}
        {
            "name": "{{ .Title }}",
            "permalink" : "{{ .Permalink }}",
            "summary" : "{{ .Summary }}"
        }
    {{ end }}
]
{{ end }}